# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Racked is a personal workout tracker: a Vite + React SPA backed by Supabase Postgres, deployed to GitHub Pages. Vitest covers the pure-logic core (`progression`, `syncQueue`, `planUtils`, `recap`, `insights`, `dataExport`, `coachUtils`, `pushUtils`, `healthSyncUtils`, `storageScope`, `equipment`, `buddyUtils`, `cycleUtils`) plus render tests (`@testing-library/react`) over the extracted view components (`ExerciseCard`, `FinisherCard`, `RestTimer`, `SessionSummary`, `PRToast`, `DayTabs`, `TravelToggle`, `ExerciseDetail`, `ui`, `useHashRoute`); ESLint (`npm run lint`, flat config, core + classic react-hooks rules only — the plugin's compiler-alignment rules are deliberately off) gates CI alongside the tests. There are no E2E tests, so flow-level UI changes are still verified by running the app (`npm run dev`) and exercising the flow in-browser (see `.claude/skills/verify/SKILL.md` for a headless recipe).

## Commands

```bash
npm install       # install deps
npm run dev       # vite dev server
npm run build     # production build (outputs dist/)
npm run preview   # preview the production build
npm test          # vitest, single run (also the CI gate on PRs)
npm run test:watch # vitest watch mode
npm run lint      # eslint, zero-warnings policy (also in the CI gate)
```

Tests live beside the code as `src/*.test.js(x)` and run in a pinned non-UTC timezone (`TZ=America/New_York`, set in the npm scripts) so UTC-drift date bugs can't pass unnoticed. The default vitest environment is node; browser-global and component tests opt into jsdom via a per-file `@vitest-environment` docblock (`syncQueue.test.js` additionally polyfills `localStorage` in-file — Node 26's stub isn't functional and vitest's jsdom env doesn't override it). Vitest globals are off, so component tests import from `vitest` explicitly and call RTL's `cleanup()` in `afterEach` themselves; never import `RackedTracker`/`AppState`/`storage`/`supabaseClient` in tests (`supabaseClient` throws without env vars). `.github/workflows/ci.yml` runs the suite on every PR and push to `main`.

Local setup requires a `.env` with `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` (copy `.env.example`); `src/supabaseClient.js` throws if they're missing. `VITE_VAPID_PUBLIC_KEY` is optional — without it the Notifications section just shows a setup hint. Full table-creation SQL for a fresh Supabase project is in `README.md`.

Deploys automatically via `.github/workflows/deploy.yml` on every push to `main` (also manually dispatchable). `vite.config.js` sets `base: "/racked/"` to match the GitHub Pages project-page URL — don't remove this.

## Architecture

**Frontend:** plain inline styles (no CSS/Tailwind), `lucide-react` for icons, installable PWA (`vite-plugin-pwa`: manifest + service worker precache + Google Fonts runtime cache; `public/push-sw.js` carries the push/notificationclick handlers into the generated worker via `workbox.importScripts` — generateSW owns `sw.js` itself, so don't try to edit it directly). No component library and no router dependency — views are hash-routed by `src/useHashRoute.js` (`#/` workout · `#/progress` · `#/plan` editor · `#/onboard` · `#/exercise/<slug>` detail overlay), which keeps them URL-addressable and the back button working in the installed PWA without server rewrites under the `/racked/` base. `#/onboard` bounces non-blank accounts back to `#/` unless the plan editor explicitly entered "replace" mode, because new-mode Skip saves the seed over the current plan.

**Shared state:** `src/AppState.jsx` (context) owns `logs`/`weighIns`/`days`/`planMeta`/`coachRuns`, the initial load, the offline-queue wiring, and the optimistic-update-with-rollback writes; `RackedTracker.jsx` is the composition + workout-session state root (active day, rest timer, swaps, PR toast) and `ProgressView` reads data straight from the context. View components stay prop-driven.

**Weekly coach (Phase 9, `src/CoachSection.jsx`):** the in-app coach review is the primary weekly surface — cached per week in `coach_runs` so the latest run renders instantly; the paste-for-Claude recap is a collapsed "raw recap" fallback below it. Suggestions apply via `handleApplyPlanChange` (rejects if the exercise isn't in the plan) with a persisted undo (`coachUtils.inversePlanChange` captures the revert before applying). `src/useAutoCoach.js` is the opt-in weekly check-in: on the first open of a new week it reviews the *finished* week (`pendingAutoReview`) and caches the result — the client-side stand-in for a server cron, off by default and device-scoped (localStorage `racked-coach-auto`). `src/coach.js` wraps the `coach` edge-function call so components never touch the Supabase client.

**Auth:** `src/AuthGate.jsx` wraps the app (in `main.jsx`) and gates everything behind Supabase magic-link email sign-in, with a one-time-code fallback (`verifyOtp`) since magic-link redirects only work against an allowlisted URL, which is brittle in dev — and, on iOS, tapping the link from Mail always opens Safari rather than the installed home-screen app (no deep-link mechanism into a standalone PWA), so `AuthGate.jsx` detects standalone mode (`navigator.standalone` / `display-mode: standalone`) and defaults straight to the code flow there.

**Email delivery:** Supabase sends auth emails (magic link + OTP) through Resend, configured as a plain SMTP relay in Supabase's dashboard (Authentication → Settings → SMTP Settings) — Resend has no template of its own in this setup; Supabase renders the email from its own dashboard template (Authentication → Email Templates → Magic Link) and just hands the finished message to Resend as a transport. Both the link and the numeric code come from that single template, which must include `{{ .Token }}` in the body for the code to actually appear (the stock template only has `{{ .ConfirmationURL }}`). Template edits can take a couple of minutes to propagate — the next email out isn't necessarily on the new template.

**Device integration (Phase 10, `src/HealthSection.jsx`):** the Progress screen's Notifications + Health sync blocks, both opt-in and fail-soft (missing tables/VAPID config degrade to setup hints, never errors). Web push: `src/push.js` owns the browser wiring (subscribe/unsubscribe, a `racked-push-on` localStorage flag so per-set rest-timer scheduling never awaits, and the `push-send` invoke — same components-never-touch-Supabase boundary as `coach.js`); `src/pushUtils.js` holds the pure/tested parts. The rest-timer push is server-delayed (`push-send` holds the request open via `EdgeRuntime.waitUntil`) and the service worker suppresses any push while the app is visible, so notifications only surface on a locked/backgrounded phone. The weekly nudge is a Sunday `pg_cron` → `push-send` call authenticated by the service role (README Phase 10). Health sync is an Apple Shortcuts bridge (no HealthKit API for web): the app mints a per-user token (`sync_tokens`), and a Shortcut POSTs weigh-ins to / GETs finished workouts from the `health-sync` edge function using that token.

**Buddy system (Phase 14, `src/BuddySection.jsx`):** two-person accountability on the Progress screen — presence only (streak, sessions vs target, "finished Push day ✓"), never set-level data. Pairing mirrors `sync_tokens`: the app mints a one-use 8-char code (`buddy_codes`, own-rows RLS), the friend redeems it through the `buddy-status` edge function (service role), which creates the single `buddy_links` row — redeeming *is* consent, no request/accept state. Unlink is a client-side RLS delete of that row (`storage.unlinkBuddy`). All buddy stats come from `buddy-status` (`src/buddy.js` wraps the invoke, same boundary as `coach.js`; `src/buddyUtils.js` holds the pure code/format helpers) — there is no client-side cross-user read path. Nudges ride Phase 10's push stack: `RackedTracker` fires `{type:"buddy-done"}` at `push-send` once when a *live* session completes (gated by the `racked-buddy-on` localStorage flag buddy.js maintains, plus `sessionStartRef` so reopening an already-complete day announces nothing), and the Sunday weekly push folds in a combo-streak line when both sides hit their weekly target. Fail-soft like Health/Notifications: missing tables or an undeployed function degrade to the mint/redeem setup UI.

**Persistence — Supabase Postgres, ten tables, all RLS-scoped to `auth.uid() = user_id` (except the service-role-only `video_links` and `fn_usage`, and `buddy_links`, which both members can read/delete but only the service role inserts):**
- `logs` — one row per logged set (`exercise_slug`, `date`, `weight`, `reps`, `effort`, `note`, `user_id`). `weight` doubles as seconds-held for timed core holds; `effort` is -1/0/1/null; `note` holds finisher machine/mode; finisher cardio is logged under per-day slugs (`finisher-a`, etc.) with `reps` = minutes.
- `weigh_ins` — bodyweight tracking, loads fail-soft.
- `plan` — one jsonb row per user (`data = {meta, days}`, pk is `user_id`). `meta` drives the recap program line, streak target, and header subtitle; `meta.cycle` (optional, Phase 15) is the mesocycle block. Pre-migration `{days}`-only rows must still load — keep fallbacks at every `meta` read site. Falls back to the bundled `exercises.json` seed if the row is missing or the read fails.
- `video_links` (`slug` pk, `url`, `title`) — shared cache of found YouTube tutorial URLs. RLS on with **no policies**: only the `find-videos` edge function (service role) touches it.
- `coach_runs` (pk `(user_id, week_start)`) — one cached coach review per week: `review` jsonb (`{narrative, suggestions[]}`) + `applied` jsonb (suggestion index → `{inverse}` for undo). Loads fail-soft: without the table the coach works live-only (no cache/history) and the weekly auto-run stays disabled so it can't spend an API call every open.
- `sync_tokens` (`user_id` pk, `token` unique) — one health-bridge token per user; the app mints/revokes it, the `health-sync` edge function (service role) resolves token → user.
- `push_subscriptions` (`endpoint` pk, `user_id`, `keys` jsonb) — one row per device/browser push subscription; `push-send` prunes rows whose endpoint the push service reports gone.
- `fn_usage` (pk `(user_id, fn, day)`, Phase 11) — per-user daily call counters behind the edge functions' abuse caps, bumped via the `bump_fn_usage` RPC (service-role only; execute revoked from client roles). Fails **open**: a missing table/RPC logs and allows, so the caps can never take a feature down.
- `buddy_codes` (`user_id` pk, `code` unique, Phase 14) — one pending buddy-invite code per user, minted/revoked in-app; `buddy-status` resolves code → owner at redeem and consumes it.
- `buddy_links` (pk `(user_a, user_b)` with `user_a < user_b`, Phase 14) — one accountability pair per user (per-column unique indexes back the one-buddy rule against redeem races). Select/delete for both members, insert via `buddy-status` only; deleting the row is the unlink.

**Storage layer (`src/storage.js`):** async `loadLogs`/`addLogEntry`/`updateLogEntry`/`deleteLogEntry`/`clearAllLogs`/`loadWeighIns`/`addWeighIn`/`loadPlan`/`savePlan`/`loadCoachRuns`/`saveCoachRun`, plus Phase 10's `loadSyncToken`/`saveSyncToken`/`deleteSyncToken` and `savePushSubscription`/`deletePushSubscription`, and Phase 14's `loadBuddyCode`/`saveBuddyCode`/`deleteBuddyCode`/`unlinkBuddy`. UI does optimistic updates with rollback + an error banner on failure. Log entries carry an `id` (Phase 12): the server pk from reads, or a client temp id (`newClientId`) minted at log time — inserts report back their pk so storage can translate temp → server id when a set logged moments ago gets edited; entries from a pre-Phase-12 snapshot have no id and render read-only. Also snapshot-caches last-successful server reads so the app cold-starts offline (server truth only; pending queued ops are layered on top at read time). All localStorage keys (snapshot, queue, opt-in flags) are scoped per user via `src/storageScope.js` — `main.jsx` sets the scope from the session before anything storage-backed mounts, and legacy unscoped keys are adopted once then removed — so accounts sharing a browser can't bleed into each other.

**Offline queue (`src/syncQueue.js`):** writes that fail on a *network* error (not RLS/SQL errors, which still surface red) are parked in localStorage and FIFO-replayed on the `online` event; new writes queue behind pending ones to preserve insert order, since progression logic reads "last entry". Amber "n entries pending sync" banner while queued. Ops are insert (`{table, row, cid?}`), update (`{kind:"update", id, fields}`), or delete (`{kind:"delete", id}`); an edit/delete of a *still-queued* insert rewrites or drops the queued op in place (`rewriteOps`, correlated by the client temp id) — the queue never carries an update/delete targeting a row the server hasn't seen, and queued updates/deletes are layered onto snapshot reads just like pending inserts.

**Progression engine (`src/progression.js`, `computeSuggestion`/`targetNumber`):** upper-body lifts +5 lb, lower-body +10 lb once the session's last set hits the top of the rep range (the last set is each session's verdict — both for hit/miss and the deload scan); timed core holds +5-10s; rep-based bodyweight moves suggest +1-2 reps; missing the target 2 sessions running triggers a 10% deload. Optional per-set effort rating shifts these: brutal+hit holds the weight and counts as a half-miss toward deload, easy+hit doubles the lower-body jump.

**Mesocycles (Phase 15, `src/cycleUtils.js`):** opt-in block structure in `meta.cycle` (`{lengthWeeks, deloadWeeks: [n], startDate}`) — week-in-block always derives from `startDate` + the Monday week key, no stored counter. `computeSuggestion` takes an optional `{cycle, date}`: planned deload weeks suggest ~90% of the working weight (anchored to the heavier of the last two sessions so mid-week recomputes don't compound the cut) and past deload sessions are skipped — never counted, never scan-breaking — in the reactive-deload miss scan. No/invalid `meta.cycle` (gated by `normalizeCycle` at every read site) reproduces pre-15 behavior exactly. Surfaced in the insight strip (week-in-block line), a workout-view deload explainer, and calendar tinting; editable in the plan editor ("Train in blocks"); the coach's `cycle_change` suggestions apply via `handleApplyCycleChange` with full-restore undo (`coachUtils.inverseCycleChange`), and `plan-designer` proposes a 4-week block for experienced lifters (startDate stamped client-side on accept).

**Edge functions (`supabase/functions/*/index.ts`, Supabase/Deno, `npm:` specifiers, JWT verification on, shared `ANTHROPIC_API_KEY` secret, model `claude-sonnet-5`):** deploy with `npx supabase functions deploy <name> --project-ref fugrbmkhuhphskitpvzc`. All except `health-sync` enforce per-user daily caps via `_shared/quota.ts` (`fn_usage` table; the CLI bundles the shared file, dashboard-paste deploys must add it manually). Quota refusals return **200 with `{error}`** on purpose — supabase-js buries non-2xx bodies and the clients already surface `data.error`.
- `coach` — weekly recap + plan (+ cycle state and the caller's local date, Phase 15) in, `{narrative, suggestions[]}` out (Zod structured output). Suggestions can carry a one-tap `plan_change` applied client-side via `handleApplyPlanChange`, or a `cycle_change` (block length / deload position / next-block start) via `handleApplyCycleChange`.
- `plan-designer` — onboarding goals form in, full `{summary, meta, days}` plan out. Server assembles ids/labels/plates and `results?search_query=` fallback video URLs post-parse; validates day count, ≥4 exercises/day, sluggable names. Experienced lifters get a proposed `meta.cycle` without a `startDate` (inert until the app stamps its local Monday on accept).
- `find-videos` — exercise names in, `{videos: {slug: {url, title}}}` out. Checks the `video_links` cache first, web-searches uncached names (`web_search_20260209`, with a `pause_turn` continuation loop), strictly validates `watch?v=` URLs, upserts the cache. Best-effort: the client always keeps its search-link fallback.
- `health-sync` (Phase 10, **JWT verification OFF** — deploy with `--no-verify-jwt`) — the Shortcuts bridge: authenticates by per-user sync token (`?token=` or Bearer), POST imports a weigh-in (same date+weight re-sends are no-ops), GET exports finished workouts (`{date, sets, lift_min, cardio_min, finisher_done}`). The GET window defaults to the last two **UTC** days — log dates are client-local, so a today-only window missed US-evening runs — with `?date=` for one exact day and `?since=` for backfills. No Anthropic involvement, no quota.
- `push-send` (Phase 10, JWT on, `VAPID_KEYS`/`VAPID_CONTACT`/`APP_URL` secrets, `jsr:@negrel/webpush`) — an authenticated user's `{type:"timer", seconds}` schedules a delayed rest-over push to their devices via `EdgeRuntime.waitUntil`; `{type:"buddy-done", day}` (Phase 14) notifies the caller's linked buddy that a session finished (day name only, quiet no-op without a link); the service role's `{type:"weekly"}` (pg_cron) broadcasts the check-in nudge, folding in the combo-streak line for linked pairs who both hit target (fail-soft: a missing `buddy_links` table just sends the plain nudge). Prunes gone subscriptions on 404/410.
- `buddy-status` (Phase 14, JWT on) — the whole buddy read path: `{action:"status", today}` resolves the caller's link and returns presence-level stats (name from the email local part, streak/week counts recomputed server-side to match `ProgressView.computeStreaks`, today's day-vote + completion from set *counts*); `{action:"redeem", code, today}` creates the link (refusing self-links and second buddies) and consumes both sides' codes. `today` is the caller's local date — log dates are client-local. User-facing refusals are 200 + `{error}`, same as the quota convention.

**Onboarding (`src/Onboarding.jsx`):** goals form → `plan-designer` call → review (tweak/regenerate, background `find-videos` upgrade, Skip → bundled seed). Shown to new users (no plan row + no logs) and re-entered via the plan editor's "replace" mode.

## Key files

- `src/RackedTracker.jsx` — composition + state root for the workout flow: wires the hash route to views, owns session state (active day, rest timer, swaps, travel profile, PR toast, backfill date), and computes day-completion. A "complete" workout requires lift sets **and** the finisher. The backfill date picker (Phase 12) re-anchors the whole workout view on a past date — set counts, finisher, completion — and suppresses the rest timer/push and PR toast while active.
- `src/ExerciseCard.jsx` / `src/FinisherCard.jsx` / `src/RestTimer.jsx` / `src/SessionSummary.jsx` (also exports `sessionStats`) / `src/PRToast.jsx` / `src/DayTabs.jsx` / `src/TravelToggle.jsx` — the extracted, prop-driven workout-view components, each with a render test beside it.
- `src/equipment.js` — travel mode (Phase 13): the fixed equipment-tag vocabulary + profiles (bodyweight / dumbbells / hotel gym, where "hotel" deliberately includes cable) and `profileSwaps`, which walks every plan day and bulk-computes the session swap map. Tags (`equip`) live on plan exercises/alts; untagged pre-13 plan rows fall back to `guessEquipment`'s name/start heuristics, and an exercise with no fitting option stays unswapped with its manual picker (surfaced in the toggle caption).
- `src/AppState.jsx` — the shared data context (see Architecture); `src/useHashRoute.js` — the hash router.
- `src/planUtils.js` — shared helpers: `SEED_DAYS`, `slug`, `exMetric`, `buildDayIndex`/`dayForDate` (one-pass date→plan-day vote; build the index once when asking about many dates), `applyPlanChange` (the coach's one-tap plan edit, inverse-matched by `coachUtils.inversePlanChange`), `finisherSlug`, category/plate colors.
- `src/cycleUtils.js` — mesocycle math (Phase 15, see Architecture): `normalizeCycle` (the read-site fallback gate), `weekInBlock`/`isDeloadDate`/`cycleStatus`/`cycleStatusLabel`, `applyCycleChange` (the coach's cycle edit, inverse-matched by `coachUtils.inverseCycleChange`). Its `cycleWeekKey` must stay in step with `recap.weekStart`.
- `src/ui.jsx` — the shared inline-style primitives (`StatBlock`, `SectionTitle`, `CopyButton`, `copyText`, `ghostBtn`, font constants, `FONTS_IMPORT`). Anything styled the same in two views belongs here, not copied.
- `src/storageScope.js` — per-user localStorage key scoping (see Storage layer).
- `src/PlanEditor.jsx` — in-app plan editor (writes to `plan`); add/remove/rename days (max 5); entry point into `Onboarding.jsx`'s "design a new plan" flow.
- `src/recap.js` — `buildWeeklyRecap({days, meta, logs, weighIns, today})` (+ exported `weekStart`/`shiftDays`), consumed by `CoachSection.jsx`.
- `src/ProgressView.jsx` — the Progress screen (bodyweight trend, streaks + consistency calendar, buddy, coach, health, export sections); `src/insights.js` + `src/InsightStrip.jsx` — the workout view's weekly insight strip (volume/stall/e1RM trends); `src/ExerciseDetail.jsx` — the `#/exercise/<slug>` overlay (chart + e1RM line; Phase 12's per-set edit/delete lives in its session list); `src/dataExport.js` — JSON/CSV export builders; `src/charts.jsx` — hand-rolled SVG Sparkline/LineChart.
- `src/HealthSection.jsx` / `src/push.js` / `src/pushUtils.js` / `src/healthSyncUtils.js` / `public/push-sw.js` — the Phase 10 device-integration surface (see Architecture); `scripts/generate-vapid-keys.mjs` mints the web-push keypair.
- `src/CoachSection.jsx` / `src/coach.js` / `src/coachUtils.js` / `src/useAutoCoach.js` — the unified weekly-coach surface (see Architecture).
- `src/BuddySection.jsx` / `src/buddy.js` / `src/buddyUtils.js` — the Phase 14 buddy surface (see Architecture); the pure code/format helpers in `buddyUtils` are the tested part.
- `exercises.json` — plan seed (`{meta, days}`) + curated substitution `alts` per exercise, all tagged with `equip` for travel-mode matching (apparatus over load: hanging/bench-apparatus core moves are `machine` even though they log bodyweight). `meta.description` must stay byte-identical to the recap's original hardcoded program line so existing users' recap text doesn't change.
- `public/icon-*.png` — PWA/app icons, generated by a dependency-free Node script (raw PNG + zlib), plate-colored per day.
- `docs/roadmap/*.md` — phase-by-phase design docs (see `docs/roadmap/README.md` for the index). Phases 1–15 + the AI-coach stretch are shipped. Check the relevant doc for the rationale behind existing behavior — including where implementation deliberately diverged from the plan — before assuming something is dead weight.

## Working conventions

- Run `npm test` after touching the pure-logic modules or the extracted view components; keep their tests current (the recap snapshot pins the paste-block format on purpose — update it only when the format change is intended). Component render tests cover the extracted pieces only — for flow-level UI changes, actually run the app (`npm run dev`) rather than relying on a type check.
- Everything reads/writes through `src/storage.js`; don't call the Supabase client directly from components.
- Any schema change needs a corresponding SQL migration documented in `README.md` (which doubles as the setup guide for a fresh Supabase project) — follow the existing pattern of ordered, copy-pasteable `alter table`/backfill blocks with a note on fail-soft behavior during rollout.
- When changing the `plan` jsonb shape, keep old rows loadable (fallbacks at read sites) rather than requiring a hard migration — this is the established pattern for `meta` and was used again for the Phase 5 per-user migration.
- **Roadmap work:** every phase so far started as a design doc in `docs/roadmap/` — for new feature work of phase size, write the doc first (intent, features, schema changes, out-of-scope), add it to the index in `docs/roadmap/README.md`, and implement against it under the conventions above. When it ships, update the index row and anything in this file it makes stale.
